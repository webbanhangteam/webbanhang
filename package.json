{
  "name": "shop-anh-thuan",
  "version": "1.0.0",
  "private": true,
  "description": "Website shop with auth, cart, product size, admin products, and payment APIs.",
  "main": "server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "tailwind:build": "tailwindcss -i ./input.css -o ./public/css/tailwind.css --minify",
    "tailwind:watch": "tailwindcss -i ./input.css -o ./public/css/tailwind.css --watch",
    "check": "npm run tailwind:build && node --check src/server.js && node --check src/utils/logger.js && node --check src/routes/auth.js && node --check src/routes/products.js && node --check src/middleware/adminMiddleware.js && node --check src/middleware/rateLimiter.js && node --check src/config/db.js && node --check public/js/script.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mysql2": "^3.22.3",
    "vietqr": "^0.0.1",
    "vnpay": "^2.5.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@tailwindcss/postcss": "^4.3.0",
    "autoprefixer": "^10.5.0",
    "eslint": "^8.57.1",
    "nodemon": "^3.1.0",
    "postcss": "^8.5.15",
    "prettier": "^3.3.3",
    "tailwindcss": "^4.3.0",
    "vitest": "^4.1.9"
  }
}
